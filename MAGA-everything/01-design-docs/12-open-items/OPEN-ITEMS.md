# OPEN ITEMS — cross-register actionable board
**Generated by:** maga-docs L1 · 2026-09-22 · INTERNAL-NO-PUBLIC · **r12 snapshot.**
**Purpose:** one table per owner answering "what is actionable *now*". The full record lives in `11-divergence/divergence-log.md` (DD-NN, docs lane) and `verification/divergence.md` (D-NN, verify lane — DD-75 crosswalk). This file is a *derived index*: every row cites its source entry; when a row and its source disagree, the source wins.
**Staleness rule:** states below are last-known at r12. `hardest/` has an uncommitted 114-level corpus + tooling (DD-109 — `DIFFICULTY.md` stale at 98), `verification/` an uncommitted r05 evidence wave (DD-112 — 37+ PNGs, D-41 pending). `02-code/` burn **committed `69f9b24`** (DD-111), `prototypes/` r6 committed `cbd0096` (DD-110), `tcg/` r3 committed `02a54cf` (DD-113) — all trees clean there. Check `git status` / `git diff` before acting on any "open" row.

## 1. ARCADE rulings owed (design decisions; nothing code-side can close)

| Item | Question on the table | Source |
|---|---|---|
| Stage size | 640×480 (docs) vs 640×400 (shipped) — both UNVERIFIED; measure reference SWF/clip, then update docs + code together | DD-01, verify D-05 |
| Scale invariant wording | Restate "integer only" as "integer ≥1x; uniform fractional <1x; never non-uniform" (shipped behavior is correct) | DD-15 |
| DM pickups | Spec says none; code now ships crates as the D-25 deadlock fix — spec update or code revert; crates provisional meanwhile | DD-18 → DD-103, DD-77 |
| DM ammo rule in original | Did 2Play DM have an ammo rule? Ruling feeds the deadlock fix's final shape | DD-77, verify D-25 |
| Wave-3 terminal victory | Endless spec vs shipped `MAX_WAVE=3` victory — loop/scale waves or label slice placeholder | DD-19 |
| Grenade owner exemption | Verify D-19 — design ruling owed (should own-grenade hurt the thrower?) | verify D-19, DD-94 |
| Weapon unlock order | Docs say pistol→shotgun→uzi; external docs say Uzi(5×)→shotgun(10×)→…→rocket(50×) — correct or mark TBD | DD-23 |
| Pistol ammo | Spec "limited ammo" contradicts original's unlimited pistol — reword acceptance hook #4 | DD-24 |
| P1/P2 keymaps | Spec proposal inverts documented original (P1=arrows, P2=WASD per CMG guide) — pre-fill dossier provisional | DD-25 |
| Devils in 2Play | Externally confirmed (ranged energy-ball); docs still gate on ARCADE — mark confirmed, add ranged recipe note | DD-26, DD-50 |
| Practice mode | Lite-faithful v1 = no practice, or ship labeled non-original convenience | DD-44 |
| Coyote/jump-buffer | Shipped (0.10s/0.06s) but original's are unresolved — fidelity toggle or ARCADE validation vs Lite | DD-45 |
| S&S2 date precision | Keep "2007", drop/flag day precision | DD-55 |
| CI episode | Spec already answered (CI2) — update dossier bullet | DD-56 |
| CI fidelity bar | Formula-replica (spec) vs parity-checklist (dossier) — state which governs v1 | DD-57 |
| Dossier capture plan | Re-anchor off dead M1.6 dependency to permitted-AG-session / native-playtest capture | DD-65 |
| Impossible dossier staleness | A-track decided (Lite v1); exact-play bar prescribes SWF vehicle — reframe as native parity | DD-28, DD-29 |
| Boxhead dossier touch bullet | Touch is v1-mandatory per spec, not a fidelity failure — reword | DD-30 |

## 2. FORGE — open in committed tree (no fix landed anywhere)

The r06–r11 burn **committed `69f9b24`** (DD-111) — every former §3 row is now committed-but-unverified pending maga-verify re-probe. What remains open:

| Item | Where | Source |
|---|---|---|
| Sub-frame pointer taps dropped (poll edge-detect) | `apps/impossible/src/main.ts` | verify D-35, DD-96 |
| Shmup HUD `WAVE 3/2` unclamped post-final-wave | `packages/shmup-core/src/render.ts:214` | verify D-36, DD-99 |
| Boss names never rendered (`pack.bosses[].name` dead data) | `packages/shmup-core/src/render.ts` | verify D-37, DD-99 |
| Replica `enemyTypes` stat-identical (cluck types differentiated; replica's 3 CHICKEN types all speed 1/hp 2) | `packages/shmup-core/src/packs.ts` | verify D-38, DD-87, DD-111 |
| Pack gift/food/enemy strings + comb/beak hardcoded (pack fields exist, render doesn't consume) | `packages/shmup-core/src/render.ts` | verify D-39, DD-99 |
| `palette.ts` + retint + asset MANIFEST regen (all colors off-token) | `apps/boxhead/src/` | DD-16 |
| Shotgun 3 pellets vs spec 5 | `apps/boxhead/src/game.ts` | DD-17 |
| Chrome unimplemented (click-to-start, legend, credits, portal) | all apps | DD-20 |
| KeyJ/KeyK double-bound (P2 versus triggers P1) | `packages/arcade-core/src/input.ts` | DD-21 |
| `Input.attach` window-level preventDefault hijacks DOM chrome | `packages/arcade-core/src/input.ts:92-99` | DD-37 |
| `PointerState` no button field → RMB missile unimplementable | `packages/arcade-core/src/input.ts:50-58` | DD-38 |
| `scale.ts` header comment self-contradictory + 4x-vs-3x cap | `packages/arcade-core/src/scale.ts` | DD-15 |
| `bh2-2p-smoke.js` cited but absent — BN2 proof not re-runnable | repo-wide | DD-53 |
| Badge wording lacks localhost/public boundary | all `apps/*/index.html` | DD-61 |
| Chrome copy-paste per app; no shared module | `apps/*/index.html` | DD-63 |

## 3. FORGE — committed `69f9b24`, awaiting verify re-probe (was the uncommitted burn, DD-94→DD-111)

Committed-but-unverified — built ≠ proven until maga-verify re-probes:

| Committed fix | Source |
|---|---|
| DM ammo deadlock → crates enabled (inverts DD-18 → DD-103) | DD-77, verify D-25 |
| sas save-aware boot + `defeated` restore + `validSave()` schema | DD-78, verify D-23 |
| sas `points=0` on save-load | DD-79, verify D-27 |
| sas `owned[]` persisted + re-buy disabled | DD-80, verify D-28 |
| sas Buckler gate L3→L2 | DD-81, verify D-29 |
| sas D-30 replay exploit closed (hub hides "Next Opponent" at `defeated≥4`) | DD-90, verify D-30 |
| sas `g.name` → `textContent` (XSS) | DD-84, verify D-31 |
| shmup pointerup/cancel → `window` | DD-82, verify D-32 |
| boxhead `'paused'` consumes Esc/P | DD-83, verify D-26 |
| cluck 3-enemy + 2-boss pack content + per-type motion/colors + authored SVGs | DD-87 |
| impossible gap-kill + front-edge side-kill — live-verified FIXED pre-commit (x=1410/x=2967); r05 PNGs re-prove post-commit | DD-96, verify D-33/D-34 |
| boxhead `gameplayDt` cap 0.05 | verify D-16 |
| `?stress` ~100 movers incl. runners (partial) | verify D-08 |
| impossible `__proto` getter object (probe hazard) | verify D-22, DD-94 |
| burger 4-pane grid + event-log dedupe + `icons.ts` authored icon set | DD-94, DD-111 |
| boxhead authored art preload (`boxhead-logo.svg`, `floor-tile.svg`) | DD-111 |
| sas 3 portrait SVGs + canvas arena scene | DD-111 |


## 4. maga-verify lane

| Item | Source |
|---|---|
| Re-probe post-burn-commit `69f9b24`: all §3 rows — DD-77/78/79/80/81/82/83/84/87/90 + D-16/D-22/D-25/D-26/D-27/D-28/D-29/D-30/D-31/D-32 (D-33/34 re-proven post-commit in r05 PNGs) | DD-111, DD-94 |
| sas claim card cross-verification (card `550e4c9` landed post-verdict) | DD-97 pattern, DD-100 |
| chicken claim card cross-verification (`aa15f76` postdates verdict) | DD-97 |
| `prototypes/MECHANICS-DIGEST.md` claims cross-verification (new r5 surface) | DD-105 |
| D-22 re-probe → closed (proto fix committed `b8ce8b6`) | DD-105, DD-89 |
| Register-side still open: D-05, D-06, D-08, D-16 | DD-89 r09 |

## 5. maga-proto lane

| Item | Source |
|---|---|
| CI mobile layout A (twin-thumb) — **committed + PROVEN** `cbd0096` (CDP multi-touch wave-1 clear, score 1200); both spec'd mobile layouts now proven | DD-110, DD-108 |
| `AudioSyncClock` (impossible music sync) — no proto coverage; the real remaining risk | `MECHANICS-DIGEST.md` §6 |
| sas thumb-arc layout — spec §Controls suggests bottom-right arc; proto used bottom strip | `MECHANICS-DIGEST.md` §6 |
| All numeric constants fleet-wide — declared guesses pending ARCADE playtest | `MECHANICS-DIGEST.md` §6 |

## 6. Sibling-surface watch items

| Surface | State @ r12 | Source |
|---|---|---|
| `tcg/` | **R3 committed `02a54cf`** — balance+seat-equity (Push +mana+draw, Guard HP −1, Thorn +0/+1 surge), 14 new `r3-*` variants, design doc updated; sim-verified sym pairs 51–56%, mirrors 45–53% P1; tree clean | DD-113 |
| `hardest/` | Corpus **114 files uncommitted** — live validator **112/114 PASS** (`109-vault-door` + `111-portal-press` autopilot sim-budget fails); `manifest.js`+`pars.js` regenerated to 114, **`DIFFICULTY.md` stale at 98**. Truth = `node hardest/validate.mjs` (~45s) | DD-109, DD-91 |
| `02-code/` | **Burn committed `69f9b24`** (25 files) — tree clean; ~14 tracked items now committed-but-unverified (§3); residual open list in §2 | DD-111, DD-94 |
| `verification/` | Register ends D-40, committed `5d3e35f`+`c6808ce`; **r05 evidence wave still uncommitted** — 37+ PNGs incl. `r05-HardestLvC-D41-menu-crash-medal-col.png` (new defect D-41 pending registration); no r05 verdict file yet | DD-112, DD-99 |
| `prototypes/` | **r6 committed `cbd0096`** — CI layout A twin-thumb PROVEN (wave-1 two-thumb touch clear), impossible practice checkpoints + input-offset calibration + full-clear re-proof (x=9902); tree clean; DD-108 RESOLVED | DD-110, DD-105 |
