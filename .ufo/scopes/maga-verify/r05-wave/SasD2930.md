# r05 SasD2930 — swords-and-sandals D-29 / D-30 verification

## Verdict
PASS — both D-29 and D-30 verified fixed on current bytes (live, http://localhost:5178, plus static read of `MAGA-everything/02-code/armor-games/apps/swords-and-sandals/src/main.ts`, 30 lines). Two NEW defects filed (D-41 high, D-42 medium).

## Evidence

### D-29 — Imperial Buckler gate 3→2: FIXED
- Static: `main.ts:10` — `Imperial Buckler … price:65, gate:2` (Lucky Sandals also `gate:2`). Level curve `main.ts:16` `1+floor(xp/40)`; win = +22 xp (`main.ts:26`) → level 2 after 2 bouts.
- Live playthrough (fresh save, gladiator "R05Verifier", str6/agi4/vit2/def2, hp32): won bout 1 vs Tin Can Tim (+18 gold), bout 2 vs Baron Bonk (+26) → XP 44 → **Lv 2**, gold 44 (hud: `SHOP · R05Verifier HP 32/32 · Gold 44 · XP 44 · Lv 2`).
- Shop control at Lv2/gold44: Lucky Sandals "38 gold · **level 2**" **enabled** (gate 2 opens at Lv2); Imperial Buckler "65 gold · **level 2**" disabled — only blocking variable is gold (44 < 65), since level 2 ≥ gate 2. Under old gate:3 both would be level-3-locked at Lv2.
- Gate isolation (labeled save fixture: gold→78 on the legitimately-earned Lv2 gladiator; xp/level untouched): reload → Buckler **enabled** → purchased → label "Imperial Buckler / **Owned**", gold 78→13, armor 0→5, log "Bought Imperial Buckler. The smith nods solemnly." Persisted save: `owned:["Imperial Buckler"], gold:13, armor:5, level:2, defeated:2`.
- Screenshots: `verification/evidence/r05-SasD2930-d29-shop-l2-gold44-control.png`, `verification/evidence/r05-SasD2930-d29-buckler-owned-l2.png`.

### D-30 — complete-screen replay exploit: FIXED
- Boot path (`main.ts:13`): save with `defeated:4` (fixture; validSave cap `main.ts:12` allows ≤4) → reload boots **straight to mode 'complete'**, h1 "V1 COMPLETE", actions = `["Return to Hub"]` only. No hub-with-button state is ever rendered.
- "Return to Hub" click → hub: actions = `["Visit Smithy / Armory"]` only; `hasStartBout:false` — no 'Start First Bout' / 'Next Opponent' (`main.ts:20` gate `if(defeated<opponents.length)`). Contrast baseline (defeated=0): hub shows `["Start First Bout","Visit Smithy / Armory"]` — screenshot `r05-SasD2930-d30-hub-baseline.png`.
- Forced-bout attempts, all dead ends:
  - `typeof startFight` → `'undefined'` (and `window.startFight` undefined) — `main.ts:23` is module-scoped, not console-reachable.
  - `window.__maga` (`main.ts:29`) = 11 descriptors, **all getter-only** (`get:true,set:false,fn:false` for mode/screen/gladiatorStats/hp/gold/xp/level/opponent/opponentHp/shop/savePresent) — no mutation or function handle.
  - DOM scan: zero bout/attack buttons anywhere.
  - Click-everything fuzz ×2 rounds: modes seen `{hub, shop}` only, `escapedHub:false`.
- Hardening: `defeated:5` tamper → validSave rejects → fresh "Create Gladiator" (savePresent true but discarded).
- Screenshots: `r05-SasD2930-d30-complete-screen.png`, `r05-SasD2930-d30-hub-after-return.png`.

## Defects (new)

### D-41 — Stored XSS via gladiator name in HUD (HIGH)
- `main.ts:17` `render()` injects raw `g.name` into `hud.innerHTML` (`<span>${…}${g.name||'Unnamed gladiator'}</span>`). Name inputs are only length-sliced (24 chars, `main.ts:19`), never escaped; save files are trusted on load.
- Live-confirmed with the pre-existing probe save found on the origin (name `<img src=x onerror=p=1>`, 23 chars): after reload `window.p === 1` and `#hud` contains a live `<img src="x" onerror="p=1">`. Screenshot: `r05-SasD2930-xss-hud-probe.png`.
- Fix suggestion: render name via `textContent` (as `main.ts:20` already does for `.gladiator-name`) or escape before template interpolation.

### D-42 — Arena ladder mathematically unwinnable from Baron onward; V1 COMPLETE unreachable through pure play (MEDIUM)
- Live-measured (automated driver, ~15+ lives across 6 chunks): vs The Sand Snorter (hp62/str10/def5, `main.ts:9`) a Lv2 gladiator (best reasonable 6-point build; guarded intake 5–7/turn, 8–10 on miss turns; output ~6–8/turn; survival ~4–5 turns = ~26 dealt vs 62 needed). Emperor's Champion (hp78/def7) is strictly worse; special damage vs def7 is 5–7 base.
- Economy: max gold before Champion = 18+26+34 = 78 (`main.ts:26` reward `18+defeated*8`); buys Buckler (65) *or* Sword+Sandals (60), never enough to flip the fight. Losses pay nothing; defeated opponents can't be refarmed (`main.ts:23` frontier `Math.min(defeated,len-1)`); xp caps at Lv2 (66) before the 4th bout, and Lv3 (88 xp) requires beating Champion.
- Consequence: mode 'complete' is reachable **only** via save tampering (as tested in D-30) — the win screen is dead content in normal play. [INFERENCE from measured attempts + combat math at `main.ts:9,16,23-26`]
- Fix suggestion: rebalance (lower Champion/Snorter defense or hp, raise xp/gain curve, or add retry gold).

## Notes
- Fixture disclosure: `gold:78`, `defeated:4`, `defeated:5` were save mutations on a legitimately-earned Lv2 gladiator (2 real bout wins) to isolate the gate and boot-path variables; all observed UI after each fixture ran the real render/boot code paths.
- Pre-existing save on the origin belonged to another lane's XSS probe (`<img src=x onerror=p=1>`); snapshotted before my run and restored **byte-exact** after (verified: name round-trips, hud re-renders it).
- Tab contamination: my first two tabs (`SasD2930`, `SasD2930-ss5178`) were each navigated to `file://…/hardest/index.html` by other lanes mid-run; once my handle resolved to the busy `HardestBoot` tab and the call was rejected before any input was sent (no foreign-tab driving occurred). Continued per Main's wave discipline with fresh names (`SasD2930-r05-t3`) and URL guards inside every page-side call. HardestBoot was notified; no Boxhead interaction.
- Evidence dir: all 6 artifacts under `verification/evidence/r05-SasD2930-*.png`.
